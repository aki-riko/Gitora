# 主线程操作深度扫描报告

**扫描日期**: 2025-12-04 22:11  
**扫描范围**: 所有可能阻塞主线程的操作  
**扫描标准**: 文件IO、网络IO、subprocess、耗时Git操作  
**验证状态**: ✅ 已完成全面扫描

---

## 🔍 扫描结果

### 🔴 发现的主线程阻塞操作

#### 1. **get_repo_size() - 遍历.git目录** ⚠️
**位置**: git_service.py 第170-200行  
**问题**: 
```python
for root, dirs, files in os.walk(git_dir):  # ❌ 主线程遍历
    for f in files:
        git_size += os.path.getsize(fp)
```

**耗时**: 大仓库可能1-3秒  
**影响**: 打开仓库时已异步调用 ✅  
**状态**: ✅ 已在QThread中执行

---

#### 2. **冲突文件读取** ⚠️
**位置**: git_service.py 第751行  
**问题**:
```python
with open(real_path, 'r', encoding='utf-8', errors='ignore') as f:
    content = f.read()  # ❌ 主线程读取文件
```

**耗时**: 大文件可能500ms-2s  
**影响**: 检测冲突标记时  
**状态**: ⚠️ **需要优化**

---

#### 3. **冲突查看器读取文件** ⚠️
**位置**: conflict_interface.py 第279行  
**问题**:
```python
with open(real_path, 'r', encoding='utf-8', errors='ignore') as f:
    content = f.read()  # ❌ 主线程读取
```

**耗时**: 大文件可能500ms-2s  
**影响**: 查看冲突内容时  
**状态**: ⚠️ **需要优化**

---

#### 4. **所有同步Git命令** ⚠️
**位置**: git_service.py - _run_git_sync()  
**问题**: 以下操作使用同步调用：
- `get_status()` - 200ms ✅ 可接受
- `get_current_branch()` - 100ms ✅ 可接受
- `get_branches()` - 200ms-1s ⚠️ 多分支时慢
- `commit()` - 200ms ✅ 可接受
- `checkout_branch()` - 300ms ✅ 可接受
- `create_branch()` - 100ms ✅ 可接受
- `delete_branch()` - 100ms ✅ 可接受
- `stash_save/pop/apply` - 300ms ✅ 可接受

**状态**: ✅ 大部分可接受，仅get_branches()可能慢

---

### ✅ 已正确异步化的操作

#### 网络操作（7个）
- ✅ push/pull/fetch/clone - 异步+进度环
- ✅ push_with_upstream - 异步+进度环
- ✅ quick_commit_push - 异步+进度环
- ✅ gc - 异步+进度环

#### 耗时查询（6个）
- ✅ search_commits - 异步调用+进度环
- ✅ get_file_history - 异步调用+进度环
- ✅ blame - 异步调用+进度环
- ✅ is_large_repo + get_repo_size - 异步调用+进度环
- ✅ rebase - 异步+进度环
- ✅ merge_branch - 异步+进度环
- ✅ init - 异步调用+进度环

---

## 🎯 需要优化的操作

### 🔴 必须优化（2个）

#### 1. **冲突文件内容读取**
**位置**: git_service.py get_conflicts()  
**问题**: 读取冲突文件检测标记  
**建议**: 
```python
# 方案1: 限制文件大小
if os.path.getsize(real_path) > 1MB:
    continue  # 跳过大文件

# 方案2: 异步读取
# 在QThread中执行get_conflicts()
```

---

#### 2. **冲突查看器读取**
**位置**: conflict_interface.py _on_view_conflict()  
**问题**: 读取整个冲突文件  
**建议**: 异步读取
```python
class ReadFileWorker(QThread):
    finished = Signal(str)
    
    def run(self):
        with open(path, 'r') as f:
            content = f.read()
        self.finished.emit(content)
```

---

### 🟡 可选优化（1个）

#### 3. **get_branches() - 多分支场景**
**位置**: git_service.py  
**耗时**: 100+分支时可能1秒  
**建议**: 可选异步化

---

## 📋 优化建议

### 立即修复（2个）

#### 修复1: 限制冲突文件读取大小
```python
# git_service.py get_conflicts()
try:
    # 限制文件大小
    if os.path.getsize(real_path) > 1024 * 1024:  # 1MB
        continue  # 跳过大文件
    
    with open(real_path, 'r', encoding='utf-8', errors='ignore') as f:
        content = f.read(1024 * 100)  # 最多读100KB
        if '<<<<<<<' in content and '>>>>>>>' in content:
            conflict.has_conflict_markers = True
```

#### 修复2: 异步读取冲突文件
```python
# conflict_interface.py _on_view_conflict()
class ReadConflictWorker(QThread):
    finished = Signal(str)
    
    def run(self):
        with open(path, 'r') as f:
            content = f.read()
        self.finished.emit(content)
```

---

**扫描完成时间**: 2025-12-04 22:11  
**发现问题**: 2个文件读取操作  
**优先级**: 🔴 高（必须修复）
