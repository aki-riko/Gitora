# 自定义图标映射建议

**分析日期**: 2025-12-04 21:00  
**图标来源**: app/resource/images/icons  
**图标数量**: 12个Git专用图标（24个文件，含深浅色）

---

## 📊 可用的自定义图标

### Git专用图标（12个）

| 图标文件 | 用途建议 | 替换位置 |
|---------|---------|---------|
| **git-branch-line** | 分支 | 分支界面导航、分支卡片 |
| **git-merge-line** | 合并 | 合并分支按钮 ✅ |
| **git-commit-line** | 提交 | 提交历史、提交按钮 |
| **git-fork-line** | 分叉/冲突 | 冲突界面导航 ✅ |
| **git-pull-request-line** | 拉取请求 | 拉取按钮 |
| **git-close-pull-request-line** | 关闭/冲突 | 冲突文件、中止操作 ✅ |
| **git-repository-line** | 仓库 | 仓库界面导航 ✅ |
| **git-repository-commits-line** | 提交历史 | 历史界面导航 ✅ |
| **git-repository-private-line** | 私有仓库 | （暂无用途） |
| **git-pr-draft-line** | 草稿 | 临时保存(Stash) ✅ |

---

## 🎯 推荐替换方案

### 高优先级替换

#### 1. **冲突界面导航** ✅
```python
# 当前
FIF.CANCEL  ❌

# 建议
Icon.GIT_FORK  ✅  # 使用git-fork-line（分叉/冲突）
```

#### 2. **合并分支按钮** ✅
```python
# 当前
FluentIcon.EMBED  ⚠️

# 建议
Icon.GIT_MERGE  ✅  # 使用git-merge-line
```

#### 3. **文件冲突状态** ✅
```python
# 当前
FluentIcon.CANCEL  ❌

# 建议
Icon.GIT_CLOSE_PR  ✅  # 使用git-close-pull-request-line
```

#### 4. **临时保存(Stash)** ✅
```python
# 当前
FluentIcon.SAVE  ⚠️

# 建议
Icon.GIT_PR_DRAFT  ✅  # 使用git-pr-draft-line（草稿）
```

---

### 可选替换

#### 5. **分支界面导航**
```python
# 当前
FIF.DEVELOPER_TOOLS  ✅ 可用

# 可选
Icon.GIT_BRANCH  ✅  # 更专业
```

#### 6. **历史界面导航**
```python
# 当前
FIF.HISTORY  ✅ 可用

# 可选
Icon.GIT_REPOSITORY_COMMITS  ✅  # 更专业
```

#### 7. **仓库界面导航**
```python
# 当前
FIF.HOME  ✅ 可用

# 可选
Icon.GIT_REPOSITORY  ✅  # 更专业
```

---

## 🔧 实现步骤

### 1. 更新icon.py定义

```python
# app/common/icon.py
class Icon(FluentIconBase, Enum):
    # 现有图标
    SETTINGS = "Settings"
    SETTINGS_FILLED = "SettingsFilled"
    
    # 新增Git图标
    GIT_BRANCH = "git-branch-line"
    GIT_MERGE = "git-merge-line"
    GIT_COMMIT = "git-commit-line"
    GIT_FORK = "git-fork-line"
    GIT_PULL_REQUEST = "git-pull-request-line"
    GIT_CLOSE_PR = "git-close-pull-request-line"
    GIT_REPOSITORY = "git-repository-line"
    GIT_REPOSITORY_COMMITS = "git-repository-commits-line"
    GIT_REPOSITORY_PRIVATE = "git-repository-private-line"
    GIT_PR_DRAFT = "git-pr-draft-line"
    
    def path(self, theme=Theme.AUTO):
        # 根据主题选择深浅色
        from qfluentwidgets import isDarkTheme
        suffix = " (1)" if isDarkTheme() else ""
        return f":/app/images/icons/{self.value}{suffix}.svg"
```

### 2. 替换使用位置

**主窗口** (main_window.py):
```python
# 冲突界面
self.addSubInterface(
    self.conflictInterface,
    Icon.GIT_FORK,  # 替换FIF.CANCEL
    self.tr('冲突')
)
```

**分支界面** (branch_interface.py):
```python
# 合并按钮
self.mergeBtn = TransparentToolButton(Icon.GIT_MERGE, self)  # 替换FluentIcon.EMBED
```

**仓库界面** (repo_interface.py):
```python
# 临时保存
stash_btn = PushButton("临时保存 (Stash)", self, Icon.GIT_PR_DRAFT)  # 替换FluentIcon.SAVE

# 文件冲突状态
FileStatus.UNMERGED: Icon.GIT_CLOSE_PR  # 替换FluentIcon.CANCEL
```

---

## 📋 完整替换清单

### 必须替换（语义错误）
1. ✅ 冲突导航: `FIF.CANCEL` → `Icon.GIT_FORK`
2. ✅ 合并分支: `FluentIcon.EMBED` → `Icon.GIT_MERGE`
3. ✅ 文件冲突: `FluentIcon.CANCEL` → `Icon.GIT_CLOSE_PR`

### 建议替换（更专业）
4. ✅ 临时保存: `FluentIcon.SAVE` → `Icon.GIT_PR_DRAFT`
5. ✅ 分支导航: `FIF.DEVELOPER_TOOLS` → `Icon.GIT_BRANCH`
6. ✅ 历史导航: `FIF.HISTORY` → `Icon.GIT_REPOSITORY_COMMITS`
7. ✅ 仓库导航: `FIF.HOME` → `Icon.GIT_REPOSITORY`

---

## 🎯 缺少的图标

### 仍需FluentIcon的场景
- 强制推送: 建议用 `FluentIcon.UP` 或保持 `FluentIcon.CANCEL`
- 文件重命名: 建议用 `FluentIcon.RENAME`（如果有）
- 撤销提交: 建议用 `FluentIcon.RETURN`（如果有）
- 回滚: 建议用 `FluentIcon.HISTORY`

---

**分析完成**: 2025-12-04 21:00  
**可用图标**: 12个Git专用图标  
**建议替换**: 7个位置  
**优先级高**: 3个
