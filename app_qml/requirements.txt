# Gitora(QML 版)依赖清单
# 安装: pip install -r app_qml/requirements.txt
# 启动: python app_qml/main_qml.py

# UI 框架
PySide6>=6.5.0

# FluentQML(pip 包名 fqml,import 名 fluentqml,MIT)
fqml>=0.2.3  # 需 Timeline virtualized 虚拟滚动(0.2.3 新增)
