# Gitora(QML 版)依赖清单
# 安装: pip install -r app_qml/requirements.txt
# 启动: python app_qml/main_qml.py

# UI 框架
PySide6>=6.5.0

# FluentQML(pip 包名 fqml,import 名 fluentqml,MIT)
# 0.2.5: Timeline virtualized + 懒加载切回不重载 + 异步孵化控制器(切页不掉帧)
# 0.2.6: Card autoHeight 内容自撑开关
fqml>=0.2.6
